# Architecture Decision Records

> **Read before:** revisiting a decision that feels arbitrary, or proposing a
> change to a foundational choice. The answer to "why is it like this?" is
> usually here.
> **Write one when:** a decision closes off a credible alternative and someone
> six months from now would reasonably ask "why not the other thing?"

## What belongs here

A decision, its context, the alternatives rejected, and the consequences —
dated. ADRs are **immutable**: they record what was decided and why *at the
time*. When a decision is reversed, write a new ADR that supersedes the old one
and add a `Superseded by` line to the original. Never edit history.

## What does not belong here

| Not an ADR | Goes in |
| --- | --- |
| An invariant that is always true | [architecture.md](../architecture.md) / [security.md](../security.md) |
| What changed in a module and when | `docs/modules/<slug>.md` build log |
| A code pattern | [conventions.md](../conventions.md) |
| A setup step | `SETUP.md` |

The distinction that matters: **rules say what is true now; ADRs say why, and
what we gave up.** A rule that has an interesting "why" gets a rule *and* an
ADR, with the rule linking to it.

## Format

Copy `_TEMPLATE.md`. Number sequentially, four digits, kebab-case title.
Keep them short — one page. An ADR nobody reads has failed at its only job.

**Add the row to the Index below in the same commit as the ADR**, with the date
and the standing copied from the ADR's own `- **Date:**` / `- **Status:**`
lines. An ADR that is not in the index is unfindable from the one page that
exists to find it, and when a later ADR changes this one's standing, both the
file and the row move. `tests/adr-index.test.ts` checks that every ADR has
exactly one row and that the row still agrees with it.

## Index

| # | Decision | Date | Status |
| --- | --- | --- | --- |
| [0001](0001-rls-force-and-separate-app-role.md) | RLS FORCE plus a non-owner `app_user` role | 2026-07-19 | Accepted |
| [0002](0002-monolith-with-module-seams.md) | Monolith with module seams, not services | 2026-07-19 | Accepted |
| [0003](0003-self-hosted-mail-over-provider-apis.md) | Self-hosted Stalwart over a hosted mail provider | 2026-07-26 | Accepted |
| [0004](0004-capability-packs-and-industry-profiles.md) | Capability packs + industry profiles, not per-industry modules | 2026-07-27 | Accepted |
| [0005](0005-polling-over-push-for-mail-freshness.md) | Polling over push for mail freshness | 2026-08-02 | Accepted |
| [0006](0006-agents-act-by-delegation.md) | An agent is a delegation from a person, not a principal | 2026-08-06 | Proposed |
| [0007](0007-cash-basis-reporting.md) | Cash-basis reporting, derived at read time | 2026-08-10 | Accepted |
| [0008](0008-warm-neutrals-and-layered-elevation.md) | Warm neutrals, layered elevation, and the navy rail stays | 2026-08-10 | Accepted |
| [0009](0009-packs-are-modules-profiles-install-them.md) | A pack is a module row; a profile installs, it does not bind | 2026-08-13 | Accepted |
| [0010](0010-entities-inside-a-tenant.md) | A tenant holds many legal entities; the entity owns the books | 2026-08-16 | Accepted |
| [0011](0011-machine-posted-entries.md) | A posting that merely records an authorised act rides that act's permission | 2026-08-21 | Accepted |
| [0012](0012-what-capitalises-stock.md) | What capitalises stock, and what joins the two records of its cost | 2026-08-21 | Proposed |
| [0013](0013-inventory-tax-treatment.md) | Inventory treatment is a policy, not a property of "cash basis" | 2026-08-21 | Proposed |
| [0014](0014-migrations-are-applied-before-the-merge.md) | Migrations are applied by hand, before the merge | 2026-08-23 | Accepted |
| [0015](0015-a-connected-account-belongs-to-a-company.md) | A connected account belongs to a company, not to the client | 2026-08-25 | Accepted |
| [0016](0016-a-catch-weight-item-is-stocked-in-packages.md) | A catch-weight item is stocked in packages and weighed on arrival | 2026-08-25 | Accepted |
| [0017](0017-the-square-account-the-farm-already-has.md) | The Square account the farm already has | 2026-09-02 | Accepted |
| [0018](0018-the-brand-kit-is-layer-0-data.md) | The brand kit is Layer 0 data, owned per company, edited by Marketing | 2026-09-04 | Accepted |
| [0019](0019-a-website-is-pages-of-typed-sections.md) | A website is pages of typed sections, hosted by the platform | 2026-09-04 | Accepted |
| [0020](0020-a-connected-domain-is-records-only.md) | A connected domain is records only; a purchased one is held for the client | 2026-09-04 | Accepted |
| [0021](0021-a-website-enquiry-lands-as-a-party.md) | A website enquiry lands as a party, a follow-up and an email, written as staff | 2026-09-04 | Accepted |
| [0022](0022-page-views-are-a-first-party-beacon.md) | Page views are counted by a first-party beacon that keeps nothing about the person | 2026-09-04 | Accepted |
| [0023](0023-photos-are-one-derivative-in-the-sites-library.md) | Photos are one derivative in the site's own library, served by the platform | 2026-09-04 | Accepted |
| [0024](0024-a-look-is-a-preset-and-its-fonts-are-the-platforms.md) | A look is a preset, and its fonts are the platform's | 2026-09-05 | Accepted |
| [0025](0025-a-booking-is-an-enquiry-with-a-time.md) | A booking is an enquiry with a time, on a calendar the platform provisions | 2026-09-05 | Accepted |
| [0026](0026-a-map-is-a-picture-the-platform-draws.md) | A map is a picture the platform draws, from public-domain tiles | 2026-09-05 | Accepted |
| [0027](0027-the-assistant-proposes-words-and-the-owner-saves-them.md) | The assistant proposes words into slots the code chose, and the owner saves them | 2026-09-05 | Accepted |
| [0028](0028-a-packs-block-is-data-the-site-draws.md) | A pack's block on the website is data the site draws | 2026-09-05 | Accepted |
| [0029](0029-the-preview-follows-the-editor-not-the-save.md) | The preview follows the editor, not the save | 2026-09-05 | Accepted |
| [0030](0030-a-site-template-is-data-an-industry-contributes.md) | A site template is data an industry contributes; the core assembles it and the writer fills the words | 2026-09-05 | Accepted |
| [0031](0031-where-a-photo-belongs-is-read-from-the-page.md) | Where a photo belongs is read from the page, never stored; the template says what to take | 2026-09-05 | Accepted |
| [0032](0032-the-mobile-app-is-the-web-app-in-a-native-shell.md) | The mobile app is the web app in a native shell; the web knows when it is inside and leaves out what the stores forbid | 2026-09-06 | Accepted |
| [0033](0033-a-setup-step-is-a-prerequisite-the-data-proves-missing.md) | A setup step is a prerequisite the data proves missing; nothing is stored, nothing can be dismissed, and a step asks "ever", not "now" | 2026-09-08 | Accepted |
| [0034](0034-a-personal-account-is-a-register-whose-ledger-leg-is-the-owners-equity.md) | A personal account is a register whose ledger leg is the owner's equity: personal by default, no opening balance, never reconciled, visible to the owner and the accountant only | 2026-09-08 | Accepted |
| [0035](0035-the-books-begin-on-a-day-and-nothing-is-dated-before-it.md) | The books begin on a day, per company; nothing may be dated before it, and an import drops the earlier lines rather than carrying history in | 2026-09-08 | Accepted |
| [0036](0036-a-pasted-list-is-proposed-by-the-model-reviewed-by-a-person-and-written-by-the-modules-own-verb.md) | A pasted list is proposed by the model, reviewed by a person, and written by the module's own verb; a target is fields plus its module's verb, choices resolve by label and never nearest, all rows or none | 2026-09-09 | Accepted |
| [0037](0037-a-document-open-when-the-books-began-is-real-and-its-other-leg-is-opening-balance-equity.md) | A document open when the books began is a real invoice or bill with one flag; its issuance is dated on the start day against Opening Balance Equity, and the cash basis recognises its lines when it is paid | 2026-09-09 | Accepted |
| [0038](0038-an-asset-owned-before-the-books-began-arrives-as-two-entries.md) | An asset owned before the books began arrives as two entries dated on the start day — its cost under `opening_balance`, the depreciation already taken under `depreciation` with the `through:` key that marks the old months posted | 2026-09-09 | Accepted |
| [0039](0039-a-pack-declares-what-it-can-be-told-in-one-sentence.md) | A pack declares what it can be told in one sentence — actions as data plus its own verb; the model picks and fills, a person confirms a card each, all of them or none | 2026-09-09 | Accepted |
| [0040](0040-the-setup-interview-runs-on-what-the-app-can-already-see.md) | The setup interview runs on a digest of what the app can already see and is forbidden to ask for it, ends in a stored plan whose steps link to real screens, and changes nothing by itself | 2026-09-09 | Accepted |
| [0041](0041-a-tenant-is-a-workspace-and-a-client-is-a-party-in-the-operator-tenant.md) | A tenant is a workspace and a client is a party in the operator tenant: Yosher runs on its own platform, the console provisions and links out, prospects live in the CRM, nothing is named Yosher | 2026-09-09 | Accepted |
| [0042](0042-a-strangers-arrival-is-a-lead-the-crm-fills-in.md) | A stranger's arrival is a lead the CRM fills in: the public doors name a slot, CRM opens the record, the deal and the note inside the door's transaction, never failing the arrival and never letting a database error escape | 2026-09-09 | Accepted |
| [0043](0043-the-platforms-revenue-is-posted-by-the-webhook-as-the-operators-owner.md) | The platform's revenue is posted by the webhook, as the operator's owner, through the ordinary invoice: one paid invoice per Stripe charge into Undeposited Funds, idempotent on Stripe's id and found again by the memo, skipped with a reason and retried rather than dropped | 2026-09-10 | Accepted |
| [0044](0044-an-industry-marketing-site-is-data-on-the-one-front-door.md) | An industry marketing site is data on the one front door, and its domain is a routing switch | 2026-09-10 | Accepted |
| [0045](0045-a-business-may-have-several-websites-and-a-kit-may-belong-to-one.md) | A business may have several websites, and a brand kit may belong to one | 2026-09-10 | Accepted |
| [0046](0046-a-preview-link-shows-an-unpublished-site-and-the-token-stands-in-for-the-slug.md) | A preview link shows an unpublished site, and the token stands in for the slug | 2026-09-11 | Accepted |
| [0047](0047-a-social-channel-belongs-to-a-website-and-a-footer-link-is-not-one.md) | A social channel belongs to a website, or to the business itself, and a footer link is not one: the site is the brand, the channel carries its audience and voice, and `other` is a channel nothing can post to | 2026-09-11 | Accepted |
| [0048](0048-a-phone-holds-a-grant-that-may-only-tell.md) | A phone holds a grant that may only tell: bound to one person in one business, never an owner, refused the moment their membership goes, with the proposal a signed blob and the phone's own clock clamped | 2026-09-12 | Accepted |
| [0049](0049-speech-is-a-fork-in-the-road-not-a-provider.md) | Speech is a fork in the road, not a provider: the phone's own engine in the app and a vendor in a browser, the probe a runtime question so an old app still works, audio stored nowhere and the transcript written only when a person presses the button | 2026-09-12 | Accepted |
| [0050](0050-a-safe-verb-records-itself.md) | A safe verb records itself: the recorder stops when the talking stops, dictation reads itself, and an action may declare a COMPLETE card of it needs no tap — visible, undoable in one step, and moving no quantity; amends 0039, which stands everywhere else | 2026-09-12 | Accepted |
| [0051](0051-the-way-in-is-the-shell-not-a-page.md) | The way in is the shell, not a page and not the nav rail: a floating button bottom-right on every dashboard page, already listening when it opens — the rail is a drawer on a phone, and a control you navigate to in order to avoid navigating is the cost it was meant to remove | 2026-09-12 | Accepted |
| [0052](0052-the-model-says-the-words-and-the-pack-goes-looking.md) | The model says the words and the pack goes looking: `find` replaces an enumerated menu, so nothing is written into the prompt and "the cows" can reach the cattle; a shortlist is a question with a line saying what each thing IS, and "never nearest" is kept by settling only what nobody could argue with | 2026-09-13 | Accepted |
| [0053](0053-a-report-is-filed-from-the-screen-it-is-about-and-its-thread-is-the-reporters-own.md) | A report is filed from the screen it is about, and its thread is the reporter's own: the route, module, viewport and shell captured rather than asked; visible to the person who filed it and to nobody else in their workspace; answered from the console under `withSystem`, with an internal note one policy clause away from the client | 2026-09-13 | Accepted |
| [0054](0054-tell-may-draft-never-send.md) | Tell may record money and may never spend it: the line is reversibility and reach, not subject matter — a confirm card cannot un-send an email or un-charge a card, so sending and paying are never proposable, and anything touching the ledger reads back its POSTING before it writes | 2026-09-13 | Accepted |
| [0055](0055-a-queued-sentence-is-old-not-wrong.md) | A queued sentence is old, not wrong: one symmetric ±15-minute clamp was answering two questions — how wrong might this clock be, and how old might this sentence be — and refused the barn-with-no-signal case it was written for; the past is believed for 48 hours, the future only for two minutes, because back-dating a clock-in pays and post-dating one does not; amends 0048's clock clause | 2026-09-13 | Accepted |
| [0056](0056-a-delivery-method-belongs-to-the-project-not-the-tenant.md) | A delivery method belongs to the project, not the tenant: construction's four flavours share fourteen of eighteen packs, so one profile ships starter project templates as rows the tenant then owns, with a CONTRACT a table many-per-project rather than a field because three of the pilot's five contract kinds are phases of one house — which makes the company doing luxury custom, semi-custom and commercial at once an ordinary tenant rather than the single-slug problem `tenants.industry` cannot answer | 2026-09-13 | Proposed |
| [0057](0057-a-pack-registers-a-seed-applier-and-the-profile-carries-the-data.md) | A pack registers a seed applier, and the profile carries the data: a profile's `seed.packs` maps a pack slug to a value the shell never types, the pack owns the shape and the applier and writes through its own ops so a seeded row obeys its rules, and `src/packs/seeds.ts` is the registry — so the console learned nothing about cost codes, and project templates are an entry in the same map | 2026-09-14 | Accepted |
| [0058](0058-a-pay-application-is-an-ordinary-invoice.md) | A pay application is an ordinary invoice, and retainage is a negative line to a receivable: issuing a draw freezes the G702 and posts it through Accounting's own verbs — the work earned this period to contract revenue, tagged with the project, and the retainage withheld as a negative line to `1230` — so AR, aging, payments and the cash lens all see it with no second ledger, and releasing retainage is the same line running the other way when a later application lowers the rate | 2026-09-14 | Accepted |
| [0059](0059-work-in-progress-is-a-snapshot-and-a-self-reversing-entry.md) | Work in progress is a snapshot and a self-reversing entry: percent complete is cost-to-cost, the one typed input is a re-estimated total cost per job per period, a period is a frozen per-company snapshot, and each period's WHOLE over- or under-billing posts per job to `1240` or `2420` against revenue and reverses the next day — so every period is self-contained and the cash basis drops the adjustment whole | 2026-09-14 | Accepted |
| [0060](0060-a-cost-plus-application-bills-the-ledger-not-the-bills.md) | A cost-plus application bills the ledger's cost to date, not the bills: what may be billed is the ledger tagged to the job, read through `getBalances` and split by cost code, billed TO DATE rather than by window so a bill posted late lands on the next application; the fee is a rate on cost to date plus a fixed fee, capped once at the guaranteed maximum; the row, the invoice and the void path are the fixed-price ones, and a job's cost is billed by one cost-plus contract | 2026-09-14 | Accepted |
| [0061](0061-a-subcontractors-application-is-an-ordinary-bill.md) | A subcontractor's application is an ordinary bill, and retainage held is a negative line to a payable: the subcontract's own lines are the schedule, an approved application goes through Accounting's `createBillDraft` → `approveBill` with the period's work to `5100` tagged with the job and the line's cost code and the amount held back as a negative line to `2120`, releasing it is the same line running the other way at a lower rate, a purchase order is refused, and one editor serves both sides of the table | 2026-09-14 | Accepted |
| [0062](0062-a-time-and-materials-application-bills-approved-hours-at-a-rate.md) | A time-and-materials application bills approved hours at a rate, and the books' cost without them: the hours are Time's, tagged with the job and on an APPROVED timesheet, each worth the rate in force on its day — the rate card's bill rate or the contract's one flat rate, locked once an application has issued — with lines keyed by person and rate and an hour whose rate cannot be found refused by name rather than given away, while the cost side leaves the `payroll_expense` accounts out so the same wages are never billed twice and the markup is on cost alone | 2026-09-14 | Accepted |
| [0063](0063-a-pay-applications-printout-is-rendered-from-the-frozen-certificate.md) | A pay application's printout is rendered from the frozen certificate, in the shape everybody knows and in our own words: the PDF reads the totals written down at issue and is never stored, a draft printing live under DRAFT and a voided one under VOID; the nine lines, the change-order summary and the continuation sheet are the arithmetic of progress billing, which is nobody's property, while the AIA's form, its text and its name appear on no page and a test scans for the word; a cost-plus or T&M application prints on the same two pages with its own lines, and any member may print | 2026-09-14 | Accepted |
| [0064](0064-a-unit-price-application-bills-quantities-at-the-schedules-prices.md) | A unit-price application bills quantities at the schedule's prices, and the schedule's value is an estimate: a schedule line gains a unit, an estimated quantity in thousandths and a price per unit — both or neither, enforced at the database — and its scheduled value is computed on save and never typed; the person types quantities and never money, rounded half up once per line and negative to correct, while stored materials stay money; the final sum is what is measured, so a line may pass 100% with a negative balance, and the application, certificate, invoice, retainage and void path are the fixed-price ones | 2026-09-14 | Accepted |
| [0065](0065-a-subcontract-change-order-adds-lines-to-the-order-and-an-issued-orders-lines-are-locked.md) | A subcontract change order adds lines to the order it changes, and an issued order's lines are locked: it is its own row against ONE commitment rather than a second kind of client change order — one number and no markup — and its money is ordinary commitment lines tagged with the change, so what an order is worth now is original plus approved changes summed through one predicate and a subcontractor bills a change's lines exactly as they bill the original ones; a deductive change is a negative line, an issued or billed-against order's lines may only move by a change order, and a change billed against is fixed | 2026-09-14 | Accepted |
| [0066](0066-a-lien-waiver-is-a-record-with-a-kind-and-a-through-date-and-the-gap-is-derived.md) | A lien waiver is a record with a kind and a through date, and the gap is derived from the payment: the words on a waiver are the state's, so the pack keeps who gave it, on which job and under which order, of which of the four kinds, through which date, for how much and whether the signed copy has been received — the copy itself a Documents attachment — and computes "paid, and no unconditional waiver on file" at read time from the application's own bill rather than storing an outstanding flag, with the chase a Work item linked to the order | 2026-09-14 | Accepted |
| [0067](0067-a-selection-is-a-decision-with-an-allowance-and-priced-choices-and-its-difference-moves-by-change-order.md) | A selection is a decision with an allowance and priced choices, and its difference moves by change order: one model — a selection carrying the allowance the contract already holds and the day it is needed by, its choices priced by the unit or as a sum with at most one marked chosen, at the database — where the chosen price less the allowance is computed wherever it is shown and, once the builder approves, raised as a change order on the selection's contract, which fixes the allowance and the choices while it stands; a selection belongs to the job and its money to a contract, and the reminder is a Work item | 2026-09-14 | Accepted |
| [0068](0068-a-subcontractors-documents-hang-off-the-party-and-the-only-behaviour-a-kind-carries-is-its-expiry.md) | A subcontractor's documents hang off the party, and the only behaviour a kind carries is its expiry: a framer's insurance covers every job he is on, so the row names the party and the Subcontractors page lists everyone holding a live order; the kind is an open taxonomy and which kinds are REQUIRED is a tenant config value, the one thing the pack does with a kind is read `expires_on`, standing is derived rather than stored, and nothing here stops a payment — the page says, and the person decides | 2026-09-15 | Accepted |
| [0069](0069-an-estimate-prices-the-job-before-anybody-signs-and-accepting-it-names-the-contract.md) | An estimate prices the job before anybody signs: cost and price are two numbers on every line, and accepting it names the contract — a line is a quantity of a unit at a unit cost sold at a markup unless a price per unit is typed, which wins, the extended figures are computed wherever shown and never stored, overhead sits on the subtotal and profit on the subtotal plus overhead, and an owner's accept writes the total onto a contract and fixes the estimate; use as budget and use as schedule of values are two further deliberate owner acts, the schedule spreading overhead and profit across the lines in proportion so it totals the contract sum | 2026-09-15 | Accepted |
| [0070](0070-a-proposal-is-the-estimate-at-its-price-and-its-words-are-fixed-with-the-money.md) | A proposal is the estimate printed at its price, and its words are fixed with the money: every line prints at its price with the estimate's overhead and profit spread into it — the unit cost, the markup, the rates and the margin appear nowhere, and a pure test scans the model for the words — in one of three presentations that all reach the same total, with a `Rounding` line for the cents unit prices leave; scope, exclusions and terms live on the estimate and are refused with the rates and the lines once it is accepted while the presentation stays free, and the document is rendered on request and never stored | 2026-09-15 | Accepted |
| [0071](0071-a-jobs-schedule-is-its-phases-as-items-on-the-business-calendar-and-a-move-pushes-what-follows.md) | A job's schedule is its phases as items on the business's calendar, and a move pushes what follows: core owns the dates — a phase is an all-day item on one business-owned Job schedule calendar, written through the scheduling module's own verbs — while the pack keeps what a calendar does not know; the dependency is finish-to-start with a lag on calendar days, moving a phase later pushes its successors each keeping its length and nothing is ever pulled earlier, because a gap the business left is theirs to close; and the pack contributes the entity-link provider that first makes a project something the platform can point at | 2026-09-15 | Accepted |
| [0072](0072-a-drawing-set-is-an-issue-of-pages-in-documents-and-the-current-set-is-derived.md) | A drawing set is an issue of pages in Documents, a sheet is a page with a number, and the current set is derived: the PDFs are ordinary cabinet documents hung on the set through the shared attach seam while the pack keeps the issue, the sheet number normalised on write, the title and the revision mark; for each number the newest issue is current and every earlier one superseded by arithmetic rather than a flag that has to be moved, the browser reads the title blocks with pdf.js and a pure rule proposes the number nearest the bottom-right corner for a person to correct, and the discipline is read from the number's first letter and never refuses one the convention does not know | 2026-09-15 | Accepted |
| [0073](0073-a-markup-is-a-vector-on-a-sheets-issue-and-a-pin-is-a-punch-item-where-it-sits.md) | A markup is a vector on a sheet's issue, and a pin is a punch item where it sits: a cloud, an arrow, a note or a pin is stored as fractions of the page and drawn as an SVG over the pdf.js canvas, so the architect's file is never touched and a cloud drawn on a phone lands where one drawn at a desk does; a pin may raise the same ordinary Work item the job's punch list uses, the key set null in the column-list form so neither side deletes the other; and a markup belongs to ONE issue, so a reissued sheet starts clean and carrying one forward is a judgement nobody makes for you | 2026-09-15 | Accepted |
| [0074](0074-a-measurement-is-a-markup-with-a-quantity-the-scale-is-the-sheets-and-a-takeoff-is-a-quantity-pushed-onto-an-estimate-line.md) | A measurement is a markup with a quantity, the scale is the sheet's, and a takeoff is a quantity pushed onto an estimate line: the sheet carries points per unit with the page's size beside it, so the server turns fractions into feet without opening the PDF and setting the scale again corrects every length and area at once; length, area and count join the markup kinds with the quantity derived every time it is read; and a push is a statement rather than an increment — one kind of thing at a time, setting the line's quantity in thousandths, refused on an accepted estimate, with each measurement remembering the line and the quantity it pushed | 2026-09-16 | Accepted |
| [0075](0075-a-change-order-and-an-order-print-from-their-rows-the-change-order-at-its-price-with-the-contract-sum-either-side-the-order-with-its-changes-beneath-it.md) | A change order and an order print from their rows: the change order at its price with the contract sum either side, the order with its changes beneath it — the lines, the codes and the word cost appear nowhere and a pure test scans the model for them, the sums read as a ladder so each approved change's "after" is the next one's "before", an order prints as placed with every change listed under it and only the approved ones in the total, and both render from the live rows because an issued order's lines are locked and an approved change's value is the agreement | 2026-09-16 | Accepted |
| [0076](0076-a-warranty-claim-is-the-record-of-a-call-its-work-is-a-work-item-the-period-is-the-jobs-and-a-claim-outside-it-is-said-never-refused.md) | A warranty claim is the record of a call, its work is a Work item, the period is the job's, and a claim outside it is said, never refused: two columns on the project give one general warranty whose expiry is derived rather than stored, the claim records what is wrong, the trade held responsible, the cost code and the covered / not covered decision, and recording it raises an ordinary Work item linked to the CLAIM rather than to the job's punch list — so where a claim stands is derived from that item, the cost is read from the job cost report for the codes its claims name, and a late call is recorded and marked outside the period, because the builder decides what the warranty covers | 2026-09-16 | Accepted |
| [0077](0077-a-back-charge-is-money-the-business-spent-that-was-the-subcontractors-kept-back-from-their-next-application-and-never-a-change-to-the-order.md) | A back-charge is money the business spent that was the subcontractor's, kept back from their next application, and never a change to the order: the subcontract still says what was agreed, so the record hangs off the commitment, always positive and numbered per order, and comes off the bottom of an application while the certificate above it stays gross — the invariant that stops the deduction being taken twice — with its standing derived from the application it rides, one negative line per back-charge on the bill tagged with the job and the cost code the money landed on, and a total at or above the payment refused by name rather than turned into a debit memo | 2026-09-16 | Accepted |
| [0078](0078-a-bond-is-recorded-on-the-job-and-a-job-counts-once-against-the-suretys-line.md) | A bond is recorded on the job, and a job counts once against the surety's line: `project_id` is required and a contract named when there is one, because a bid bond exists before any contract does; the bonds are folded per job before the arithmetic sees them, since a surety backs the work rather than the pieces of paper; what is used is backlog — the contract sum less what has been billed, floored at nothing — tied up from the day a bond is asked for and given back only by releasing it; the limits are a row per legal entity an owner types, the kind is an open taxonomy, and the premium is recorded and never posted | 2026-09-16 | Accepted |
| [0079](0079-an-estimate-groups-its-lines-into-the-items-the-client-sees-and-a-group-priced-fixed-is-the-price-not-a-cost-to-mark-up.md) | An estimate groups its lines into the items the client sees, and a group priced fixed is the price, not a cost to mark up: a group is one level deep and carries the client's words so the estimator's line need not, and its price either rolls up or is typed — a typed price being excluded from the overhead-and-profit spread, so the number typed is the number printed to the cent; cost is always the lines', so the margin tells the truth and the budget does not change at all; the schedule of values now follows the groups by default, and a fixed group prints as one row at its price in every presentation, because typing a price is itself the statement that the build-up is not the client's business | 2026-09-16 | Accepted |
| [0080](0080-an-estimate-line-carries-the-clients-words-beside-the-estimators-and-a-line-kept-off-the-proposal-collapses-the-item-that-holds-it.md) | An estimate line carries the client's words beside the estimator's, and a line kept off the proposal collapses the item that holds it: a blank client description means use the estimator's, and the one on the line reaches both the proposal and the schedule of values the owner later certifies; a line may be hidden only INSIDE an item — a CHECK refuses it on a loose line, because hidden money must have somewhere to hide — and one predicate collapses an item that is priced by hand or hides any of its lines into a single row, which is the whole of the arithmetic; hiding is about not itemising rather than concealment, and the cost code's number is off by default | 2026-09-16 | Accepted |
| [0081](0081-an-estimate-line-can-be-typed-as-one-sentence-and-the-grammar-that-reads-it-is-pure-and-refuses-what-it-cannot-read.md) | An estimate line can be typed as one sentence, and the grammar that reads it is pure, learns its units, and refuses what it cannot read: one pure parser serves three doors — the entry bar, the paste box, and the phone the day it hears a line — because a second door is never a second pipeline; it is taught the trade's own abbreviations unioned with every unit this business has already typed, a hint to a parser and never a validation; and a sentence it cannot read returns null rather than a line with a silent zero in it, since the platform's paste-target framework describes a target per TENANT and writes one row through a module's verb, which an estimate line is not | 2026-09-16 | Accepted |
| [0082](0082-an-estimate-saves-itself-unsaved-is-derived-from-the-form-and-a-save-that-changes-nothing-writes-nothing.md) | An estimate saves itself, "unsaved" is derived from the form rather than flagged, and a save that changes nothing writes nothing: the form autosaves 1.2 seconds after typing stops with no toast and no revalidation, dirtiness is the stringified payload compared with what was last sent rather than twenty `setDirty` calls one of which is eventually missing, and the version rides along and must never be compared — comparing it made every successful save dirty against its own reply; a save whose rows already hold every value being written updates nothing, moves no version and writes no audit row, which is what makes a timer safe, and a failed autosave is as loud as a failed save | 2026-09-17 | Accepted |
| [0083](0083-a-proposal-is-an-ordered-list-of-sections-a-brochure-is-a-page-order-over-facts-the-pack-already-holds-and-the-document-is-html.md) | A proposal is an ordered list of sections, a brochure is a page order over facts the pack already holds, and the document is HTML: a `format` column picks the page order while the presentation stays orthogonal to it, the brochure's pages come from the items' names and notes, the job's selections and its phases — two columns added, no new table — the sections WRAP the one price model rather than computing a second total, a page with nothing on it is not printed, and the document is served as HTML from a GET route with an inline stylesheet and a data-URI logo, deliberately at the same URL the PDF and the client link are built on | 2026-09-17 | Accepted |
| [0084](0084-the-brochures-pdf-is-a-headless-print-of-the-document-itself-and-the-format-picks-the-engine.md) | The brochure's PDF is a headless print of the document itself, and the format picks the engine: one `/pdf` address answers with react-pdf for a letter and a headless browser for a brochure, and the press is handed the rendered HTML rather than a URL, so both doors return the same expression and nothing has to carry a session into Chromium; where the browser comes from is one pure function over the environment, a local one beating a pack it would have to download; a print that cannot run answers 503 with a page rather than silently falling back to the letter; and the running footer printed through the text until an empty `tfoot` reserved the band — tfoot reserves, fixed pins — which only paper could show, so the print now has a probe | 2026-09-17 | Accepted |
| [0085](0085-a-client-link-is-a-tokenised-copy-of-the-proposal-and-accepting-on-it-records-a-signature-rather-than-accepting-the-estimate.md) | A client link is a tokenised copy of the proposal, and accepting on it records a signature rather than accepting the estimate: the credentials are `document_shares`' verbatim — a 256-bit token kept only as a keyed HMAC and as ciphertext, failing closed with no secret — `withSystem` does the token-to-tenant hop and nothing else while every read after it runs under `withTenant` at staff, and every failure answers the same unbranded sentence so a visitor learns nothing; the signature is five columns or none, checked against the version the client was shown, written once, with its standing derived and `superseded` a fact about two rows; the link expires when the offer does, and no server-printed PDF is served on a public address | 2026-09-17 | Accepted |
| [0086](0086-an-assembly-is-an-item-saved-at-the-size-it-was-priced-and-only-the-quantity-scales.md) | An assembly is an item saved at the size it was priced, and only the quantity scales: it is the same shape as an estimate item, saved from the editor's own state and recording the driving quantity and unit it was priced at, with the quantities kept AS THEY WERE rather than as ratios because `6 bags at 320 sf` is a number an estimator recognises; dropping divides once, and scales the quantity alone — a unit cost, an explicit unit price and a markup are already per unit, and scaling a rate is the one mistake that produces a plausible wrong number in a proposal; the cost code travels as text and resolves against the target job's own set, where no match means NO code rather than a near one | 2026-09-18 | Accepted |
| [0087](0087-a-row-on-an-estimate-has-a-number-and-the-number-is-its-address.md) | A row on an estimate has a number, and the number is its address: items are `1`, `2`, `3`, a line is `<item>.<place>`, and the loose pile is numbered as the section after the last item, so typing a number over a row moves it there — a line crosses into another item by its number, a number past the end is clamped rather than refused, and anything the grammar cannot read puts the old number back; dragging does the same thing through one pure ordering module that normalises to visual order before it returns, so the screen and `sort_order` cannot disagree, and nothing changed in the schema, the action or the payload because autosave already persisted the order | 2026-09-18 | Accepted |
| [0088](0088-the-page-column-is-100rem-and-a-sentence-is-80ch-and-those-are-two-different-numbers.md) | The page column is 100rem and a sentence is 80ch, and those are two different numbers: one token caps how wide a PAGE gets — roughly twice the old column, and a cap rather than full bleed, because past some width a table row stops being a row and becomes a journey — and a second caps how wide a SENTENCE gets, in `ch` so it scales with the type; the sentence cap is two CSS rules scoped to the app shell rather than 398 edited call sites, with `max-w-none` the opt-out for a paragraph that really is a layout row; and form controls are deliberately not capped in CSS, because no width is right for both a domain name and a takeoff line | 2026-09-18 | Accepted |
| [0089](0089-a-job-tab-is-config-not-a-pack-and-switching-one-off-never-hides-work.md) | A job tab is config, not a pack, and switching one off never hides work: the tabs are facets of one record rather than capabilities, so they live in the pack's own `tenant_modules.config` — with Overview, Contracts and Job cost refused outright — stored as what is OFF, so a tab added next year is on for a business that configured this today rather than silently missing; a tab that has rows on the job being looked at is drawn whatever the setting says, because the setting decides what a job STARTS with and not what it is allowed to remember; the page stays reachable by URL since hiding is a preference and not a permission; and the write is owner-only under `withSystem` with the rest of the config read and spread back | 2026-09-18 | Accepted |
| [0090](0090-which-side-of-the-business-the-rail-shows-is-a-view-and-the-books-are-not-in-it.md) | Which side of the business the rail shows is a view, and the books are not in it: a nullable `entities.industry` that no query filters on, no policy reads and no report groups by — its entire effect is that the rail can put away the packs the chosen side does not use — and it is deliberately labelled by INDUSTRY rather than by company, because accounting's `?entity=` picker is URL state where a bare link means all companies for everybody, and a rail preference driving it would show two people different numbers on the same link; the shell filters tools and the page filters books, the preference is a cookie because the rail is server-rendered, and the control never renders below two industries or hides the page you are on | 2026-09-18 | Accepted |
| [0091](0091-a-division-carries-its-own-pack-list-because-a-division-is-not-an-industry.md) | A division carries its own pack list, because a division is not an industry: `enterprises.packs` is a `text[]` an owner ticks, where a company's tools are an INFERENCE from its trade and a division's are a CHOICE because nothing can infer them, and both become the same rail context before anything reads them; core tools are never in the list, because every division posts to the same books, raises the same documents and sends the same mail; empty means not said and so is never a side of its own; and the context key is prefixed by kind — `industry:<slug>` against `division:<uuid>` — because both share one cookie and must not collide | 2026-09-18 | Accepted |
| [0092](0092-a-company-may-override-the-tools-its-trade-implies.md) | A company may override the tools its trade implies, because a trade is not a business: `entities.packs` is the same `text[]` one rung up, with the industry inference left as the default and empty still meaning not said, so a company that has named its own tools splits out of its trade's row and is labelled by its own name with the trade beneath it; core tools are never in the list, because every company here is in one workspace sharing a chart of accounts, contacts, mail and documents; and the Companies screen renders even above a single company, because the button to make a second must not live on a list that needs two | 2026-09-18 | Accepted |
| [0093](0093-what-somebody-may-open-is-a-gate-which-company-s-rows-they-may-read-is-postgres.md) | What somebody may open is a gate; which company's rows they may read is Postgres: Reports, the journal and a bill's detail page all read the same rows and Postgres has no concept of which screen is asking, so areas are a server-side gate in one predicate and companies are RLS; an access level is a named set of what somebody may NOT reach, nullable on the membership so everybody today keeps the access they have, stored as what is off so next year's screen is reachable rather than silently missing, and a denied module denies every area in it; the check goes inside `requireModuleEnabled`'s 349 call sites rather than 349 edits and one forgotten hole, both halves answer `notFound()`, policies close rewriting your own level or your own membership, and deleting a level in use is refused rather than cascading everyone on it to unrestricted | 2026-09-19 | Accepted |
| [0094](0094-a-company-scope-is-resolved-by-the-transaction-not-passed-to-it.md) | A company scope is resolved by the transaction, not passed to it: `memberships.entity_ids` is a `uuid[]` where empty means every company, and because `withTenant` has 877 call sites an optional argument granting everything when omitted would be a boundary with 877 chances to be forgotten, silently, in the direction that leaks — so the transaction reads the caller itself from Clerk's `auth()`, into a GUC of its own rather than the mail seam's, whose contract is that a caller who forgets it sees nothing; 49 tables then gain ONE `AS RESTRICTIVE` policy each and keep every policy they had, with 31 child tables inheriting through their parent, a null parent allowed rather than denied, the superadmin short-circuit first because leaving it out stops every webhook, cron, seed and migration, and the scope computed once per transaction inside the `set_config` already issued | 2026-09-19 | Accepted |
| [0095](0095-a-tool-declares-its-own-parts-and-the-gate-reads-the-path.md) | A tool declares its own parts, and the gate reads the path: areas are declared beside a tool's slug and icon, filled in from the real route tree rather than a central list somebody has to remember to edit, with a one-screen tool left all-or-nothing and the tool's front door never an area, so a level that takes every area away still leaves a working overview; the gate derives the area from the `x-yosher-path` the proxy stamps over anything a client sent, which is how all 349 call sites gained it without being edited — for a server action that path is the submitting page's, so the area check is defence in depth and never the boundary for a write — and the menu is filtered once on the server into route prefixes the client compares, never hiding the section you are standing in | 2026-09-19 | Accepted |
| [0096](0096-a-route-handler-is-a-door-too.md) | A route handler is a door too: `routeGate` returns null or a `Response`, because a handler cannot `notFound()`, and asks all three questions in the order they matter — has the business this tool, may this person reach it, may they reach the part of it this route serves; an API route is not under `/dashboard/m/`, so nothing can derive its area and the route names it, taken from the screen that LINKS to the route rather than from the route's own name; `areas` is any-of, because a file linked from two tabs has a legitimate route from either; and a route with no signed-in caller is listed in `NOT_A_SESSION` with what authorises it instead, since an allowlist nobody has to justify is a hole with a comment on it | 2026-09-19 | Accepted |
| [0097](0097-accounting-splits-to-the-leaf-and-a-level-has-somewhere-to-start.md) | Accounting splits to the leaf, and a level has somewhere to start: every section with children gets them, twelve areas becoming twenty-six, and a section's landing page belongs to no area and redirects past what the reader cannot open — giving it to Invoices was tried first and was worse, stranding Customers behind a door nobody had — while the Reports index and the filter pills filter their own links, because a tile onto a page that refuses makes a permission screen worthless; and five starters are offered while a level's name is still free, each creating an ordinary level with nothing of the starter left but a name, its denied list computed on the server from what that business actually has, and named after the job rather than the trade | 2026-09-19 | Accepted |
| [0098](0098-an-estimate-outline-is-the-tenants-and-a-question-is-a-row-so-an-answer-can-point-at-one.md) | An estimate outline is the tenant's, and a question is a row so an answer can point at one: three tenant-owned tables seeded from a profile the way cost code lists are and editable to nothing, because a remodel is not a new build and a pack must never carry one business's sizes or prices — the construction starters ask and never answer; a cost code travels as text and is matched when the interview reaches a job, with no match meaning no code rather than a near one; a question is a row rather than a string in a `jsonb` array, so an answer can point at it, an interview can be resumed exactly and a builder can ask in six months why a bid never covered the sump; nothing branches, because coverage is the outline's job and judgement is the interview's; and there are two flags, since ours and theirs are two different statements | 2026-09-19 | Accepted |
| [0099](0099-a-walk-is-finished-when-nothing-is-outstanding-not-when-the-questions-run-out.md) | A walk is finished when nothing is outstanding, not when the questions run out: a walk carries a RECKONING — every step of the outline in one of six standings, derived on demand from the answers, the lines that reached the estimate and the bids that went out — so the screen stays put when the questions run out and shows what is left, and the estimate says the same thing on the way back in; money out for bid is not in the total, neither the lowest nor the average; going back supersedes an answer rather than deleting one, which re-opens its step and is why no "a person is revisiting" mode exists anywhere in the code; and the must-ask guard polices the walk, not the person | 2026-09-20 | Accepted |
| [0100](0100-a-measurement-is-a-fact-about-the-building-not-an-answer-to-a-question.md) | A measurement is a fact about the building, not an answer to a question: `job_measurements` hangs off the PROJECT keyed by the reduced name, so re-measuring corrects one row and a second estimate starts with the building already measured — and because there are only a handful they go into EVERY turn's prompt, which is what stops a walk that was told 2,400 square feet at framing forgetting it by drywall; what to measure is the tenant's, a list beside the steps and the questions, collected once before the first phase and stamped rather than derived so editing the outline cannot drop a running walk back into measuring; the drawings open over the walk in the same `SheetViewer` behind one optional prop, offering only traces of the kind being asked for and their total; and the model is nowhere near it — the parser reads `38'-6"`, `24 x 40` and `40 + 24 + 40 + 24`, refuses two figures with no operator, and never reads a minus sign because `38-6` is feet and inches | 2026-09-20 | Accepted |
| [0101](0101-a-room-is-a-name-a-floor-and-an-area-and-one-answer-is-shared-out-across-them.md) | A room is a name, a floor and an area, and one answer is shared out across them: not a rich object, because fifteen rooms times five attributes is the same trap as fifteen questions moved to another screen — and its area is a `job_measurements` row scoped to it rather than a column, so it is read by the parser that reads `24 x 40`, traced with the same dialog, and a room's WALL area later is a row rather than a migration; a room is unique per FLOOR because a house has two bathrooms; the walk asks "what flooring is going where?" ONCE, says back which room gets what and names the rooms the answer missed, and the proposal writes one line per finish with the rooms' areas added up in `derivedFrom`; the list arrives as a paste, split on a tab, a comma or a wide gap but never a single space, with a trailing colon meaning a floor; and `askNextMeasure` stopped stamping `measured_at`, which had been skipping the rooms question entirely on a building somebody had already measured | 2026-09-21 | Accepted |
| [0102](0102-a-person-picks-a-finished-walk-back-up-and-a-phase-lands-in-the-item-it-already-made.md) | A person picks a finished walk back up, and a phase lands in the item it already made: 0099 said a walk is finished when nothing is outstanding and the code then broke that rule twice — `applyAndMoveOn` asked only what came AFTER this phase, so a walk taken to the ninth step from the rail closed itself over eight nobody had been asked about, and every door back into a finished one refused it, so the reckoning named holes on a screen where none of them could be acted on; the two doors a CLICK reaches now put a finished walk back to running (never an abandoned one, which somebody stopped on purpose) while every other write still refuses a closed walk, `onwardStep` falls back to the earliest outstanding phase so null really means the whole outline is covered, pricing is guarded by the phase having finished rather than by the walk being open, and applying a phase again lands in the item it already made — found through the `estimate_line_id` the row kept and never through the name, because two items may share one and a person may rename the walk's — instead of appending a second copy of the phase to the total | 2026-09-21 | Accepted |
| [0103](0103-a-step-names-the-item-it-always-makes-and-the-item-says-whether-it-is-one-line-or-a-line-per-room.md) | A step names the item it always makes, and the item says whether it is one line or a line per room: an assembly was the answer to *"how do I get the consistent items I want with the verbiage I want"* and had been since E6, but the MODEL decided whether to reach for one, and whether four rooms were one line or four was a rule in the same prompt, re-decided from scratch on every bid — both are facts about the business, so `assembly_id` goes on the outline step and `line_shape` on the assembly, with null the normal case and the picker not even rendered until a business has assemblies; the rooms a line covers become a FIELD the software composes the description from, in the building's own spelling, because you cannot split *"Tiled shower — master bath, hall bath"* in two without knowing where the name ends; `per_room` promises one line per room sized by that room's floor area ONLY when the item is priced in the unit the rooms are measured in, and one of it otherwise, rather than dividing a total nobody stated between them; nothing computes a `one_line` quantity, since floor area is right for flooring and wrong for paint and a second invisible way to the same figure is two answers to one question; and the pin lands on the first line that has not claimed an assembly, never over one that has | 2026-09-21 | Accepted |
| [0104](0104-an-item-can-be-an-allowance-and-accepting-the-estimate-makes-it-a-selection-at-the-price-the-client-signed-for.md) | An item can be an allowance, and accepting the estimate makes it a selection at the price the client signed for: the machinery for reconciling an allowance had been in the pack since slice 8 and had no way of STARTING, so every allowance was read off a signed proposal and typed into Selections again by hand; the flag goes on the ITEM rather than the line because an allowance is a promise about a price and a promise needs a name and one number, and on the ASSEMBLY too so plumbing fixtures never have to be remembered per job; the client reads it in the words — *Plumbing fixtures (allowance)*, composed once where every format's rows are built rather than as a flag a renderer could forget; and the figure written to the selection is the item's SCHEDULED price, marked up and carrying its share of overhead and profit, on the founder's own rule that *"the allowance is a cost we mark up like everything else"* — taking the cost would give away the margin on every allowance in the job and leave the later change order comparing a price against a cost; the link is an id, so accepting twice cannot make two and a renamed allowance is still the same one | 2026-09-21 | Accepted |
| [0105](0105-a-question-can-carry-the-answer-this-business-always-gives-and-the-walk-states-them-all-before-it-takes-any.md) | A question can carry the answer this business always gives, and the walk states them all before it takes any: thirty-three phases asking who is doing each one is thirty-three questions with one answer against a target of a bid in forty-five minutes, so `standard_answer` goes on the QUESTION — blank by default, blank meaning ask, and set per question because the same prompt on the roofing step staying blank is the difference between skipping what never varies and assuming what does; nothing is taken until it has been read, so the walk states every standard before the first phase and waits, grouped by what they SAY rather than listed per phase because thirty-three identical lines is a rubber stamp; each is then settled as its phase opens and only while still outstanding, carries `from_standard` and reads *your usual* on the screen and in the reckoning, and `always_ask` refuses one at both the write and the read; three states in two columns, since a *no* has to be as durable as a *yes* — and driving it found the third way out of measuring, the rooms answer, which every measuring outline actually leaves by and which had no gate at all | 2026-09-21 | Accepted |
| [0106](0106-a-takeoff-can-be-read-off-the-models-own-schedule-and-the-boundary-is-rows.md) | A takeoff can be read off the model's own schedule, and the boundary is rows: the founder's first answer on the first day of this layer was *they draw in Revit*, and after a whole walked bid the pilot's data read three measurements typed and eleven room areas typed with a takeoff opened once, so the measure-up now takes the schedule export every Revit user already knows (*File → Export → Reports → Schedule*) rather than the `.rvt` (impossible) or the IFC (weeks before the first number); the text becomes a table and everything after — column kinds, the room list, suggestions, figures — reads the TABLE, so an IFC reader or a typed spreadsheet is a second reader and never a rewrite; it lands on X7's measurements and X8's rooms as `source = 'schedule'` with a note naming the schedule, column and rows; a column can honestly offer its total, the figure every row shares, or the row count, and identifier columns are never offered; a measurement is SUGGESTED when every word of its name is in the title or header and in the same dimension, never decided; units convert only within a dimension and a length handed to an area is refused by name; the model is not in the path; and the walk carries on through the same `afterMeasuring` / `answerRooms` a typed answer uses, nothing refused quietly | 2026-09-21 | Accepted |
| [0107](0107-a-takeoff-off-the-model-is-a-join-through-the-assembly-keyed-by-what-the-model-calls-it.md) | A takeoff off the model is a join through the assembly, keyed by what the model calls it: the founder's *"lumber takeoff, drywall etc from the model"* is the estimate itself, not the measure-up's numbers, and a schedule off the model is things with quantities — so it is grouped by the column that names things and joined to the assemblies, which were already parametric (0086) and only lacked the join; the join is `job_assembly_keys`, one row per name the model has used pointing at the assembly it means, unique per tenant on the reduced name so a name means one assembly and a re-mapping is a correction; a remembered name maps itself and the words only suggest, because a takeoff that guessed which assembly a wall type meant would price the wrong wall with perfect confidence; the figure is the row's in the assembly's own dimension, converted into its unit, and a row with no figure in that dimension is refused by name; a thing no assembly makes still comes in as a plain line by a figure the person picks; every line carries a basis with the whole story; and nothing writes an estimate line — the editor appends the items (0082) and the one thing written on confirmation is what the name means | 2026-09-21 | Accepted |
| [0108](0108-a-phase-the-estimate-already-has-is-stated-at-the-gate-and-ends-in-the-money-it-has.md) | A phase the estimate already has is stated at the gate, and ends in the money it has: the takeoff off the model (0107) puts drywall on the estimate before the walk starts and the walk could not see it — it asked, proposed, and the estimate carried the drywall twice; a phase's lines are the ones on its cost code or in the item its pinned assembly makes, both facts the business stated on the step and never a guess from words, and the walk's own lines are left out through the proposed rows that remember them; the covered phases are a second section of 0105's gate — *and these phases are already on the estimate, so I will move past them* — because that gate already asks the one question this is and a second gate would be a second tap for it; agreed, a covered phase's questions settle as it opens, every one but a must-ask, skipped with the reason on them; whether agreed or refused, a covered phase ends in the money it has and is never proposed again, and the reckoning counts it as priced at what the estimate carries, or as a hole when every one of those lines is unpriced; the estimate is read fresh at the gate, as each phase opens and as it finishes, because a takeoff can land mid-walk | 2026-09-22 | Accepted |
| [0109](0109-a-line-is-measured-from-where-it-is-priced-and-what-stands-behind-it-follows-it-across-sheets.md) | A line is measured from where it is priced, and what stands behind it follows it across sheets: every estimate line a drawing can measure carries a ruler that opens the job's sheets FOR that line, each sheet saying what already stands behind it there and every trace of the line's kind carrying a tick; what stands behind a line is a set of traces on any of the job's sheets and the line's quantity is their total, added up across sheets in the dialog; the claim (`standBehind`) writes the link and each trace's own share and nothing else, because the editor holds the estimate (0082) and sets the quantity itself; the reverse link (`measurementsBehind`) is read off the markups, never stored, so the chip naming the sheets cannot disagree with the drawings; a push from the sheet page still states the whole set but lists the line's traces on other sheets, ticked; and a save hands the editor the ids it minted, because until then a new line's id churned on every autosave and cut anything hanging off it | 2026-09-22 | Accepted |
| [0110](0110-a-trace-yields-the-figures-the-trade-derives-from-it-and-each-can-stand-behind-a-line-of-its-own.md) | A trace yields the figures the trade derives from it, and each can stand behind a line of its own: what the trade types onto a trace — a height on a length, a pitch or a depth on an area, the openings cut out of it — is kept on it as typed (`figures` jsonb, read tolerantly) and every derived figure is worked out when read through the sheet's scale, never stored, so a corrected scale corrects the wall, the roof and the volume as it corrects the area; an area yields its NET area, the run around it and, with a pitch or a depth, a roof and a volume, a length yields its length and with a height the wall it stands; every figure belongs to a family a line's unit calls for, and `cy`/`m3` call for a volume; the link moves off the markup into `job_estimate_line_traces`, one row per (line, trace, figure) with its own share, so one room traced once feeds the flooring by its area, the baseboard by its perimeter and the slab by its volume; a figure a trace does not yield is refused by name; the old columns stay unwritten until a later migration drops them | 2026-09-22 | Accepted |
