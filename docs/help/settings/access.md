# Access

> Levels: naming what a job can open, and giving it to people. What a level does and what it does not do yet.
> **Route:** /dashboard/settings/access
> **Order:** 2

Open **Access** under `Settings` in the sidebar. Owners only.

Everyone on your team reaches every tool you have switched on. That is the starting point and it does not change on its own. A **level** takes some of it away: you name a job once — `Field crew`, `Bookkeeper`, `Shop lead` — tick what that job can open, and put people on it from the Team page.

**Owners always reach everything.** A level can only be given to staff and to your accountant. If you want to limit someone who is an owner, make them staff first.

## What you see

- **The heading** — `Access`, with `What each person can open. Everyone starts able to reach everything you have switched on; a level takes things away.` and {button:Add a level|primary}.
- **The table** — `Level`, with its notes underneath if you wrote any. `Cannot open`, the tools that level does not get, or `Nothing — reaches everything`. `People`, how many are on it. `Actions`: {button:Edit|ghost} and {button:Delete|ghost}.
- **When there are none**, the page says `No levels yet` with the same definition and one button.
- **The card at the bottom** says three things plainly, and they are worth reading before you rely on this. It is rewritten whenever any of them changes.

## How to make a level

1. {button:Add a level|primary}.
2. **`Name`** — what the job is called. `Field crew`. Required, up to 60 characters, and different from your other levels.
3. **`What they can open`** — a tick box for every tool you have switched on. **Everything starts ticked.** Untick what this job does not need.
4. **`Notes`** — why it exists, for whoever inherits it. Staff never see this.
5. {button:Add|primary}. It appears in the table.

**Ticked means they can open it.** Untick `Reports` and people on that level do not get Reports: it is gone from the menu down their left, and if they type the address the page answers as if it were not there.

**Overview, `What needs you`, Guides, their own hours and their own phone are always there.** They are not tools you switched on; they are how a person uses the product at all.

**A tool you have not switched on is not in the list**, because there is nothing to take away. If you switch it on later it is reachable by everyone until you edit the level.

## How to put somebody on a level

Levels live here; people go on them on the **Team** page, under `What people can reach`. Pick the level beside their name, or `Everything` to take them off it. See the Team guide.

Changes reach them on their next page load. Nobody is signed out.

## How to change or delete one

1. {button:Edit|ghost}. The dialog says how many people are on the level, so you know who this reaches before you change it. Same fields as adding.
2. {button:Delete|ghost}. It asks first.

**You cannot delete a level while anybody is on it.** Move them to another one, or to `Everything`, first. This is on purpose: if deleting a level quietly took its restrictions off, the people on it would silently get the run of your workspace, which is the one way this must never go wrong.

## What a level does, and what it does not

**It decides which screens somebody can open.** Not just what is in their menu — the pages themselves refuse.

**It does not yet limit which company's books they see.** Somebody who can open a report can still run it across every company you keep. That is the next piece of this, and it works differently underneath, because for figures it has to be the database that refuses rather than the screen.

**It does not change what anybody has already done.** Records, entries and documents are exactly where they were, whoever made them.

**It is not a password.** It decides what a person who is already signed in to your workspace can reach. Someone who should not be in your workspace at all is removed on the Team page.

## Who can do what

Owners only, to see this page and to change anything on it — including putting people on levels. Staff and accountants never see it, and cannot change their own level or anybody else's.
